# hermes-cys

Hermes Agent 完整代码存档：

- `hermes-agent/`：Hermes Agent 完整源代码及本机修改
- `custom/plugins/`：自定义插件
- `custom/memory_dashboard/`：自定义记忆框架面板
- `custom/skills/`：已安装技能及其脚本
- `custom/launcher/`：自定义启动脚本

为防止泄露和仓库膨胀，不包含 API 密钥、认证信息、用户数据、会话、记忆数据库、日志、缓存、虚拟环境、依赖目录和备份文件。
